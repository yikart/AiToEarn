// 发布类型
export enum PubType {
  VIDEO = 'video', // 视频
  ARTICLE = 'article', // 文章
  ImageText = 'image-text', // 图文
}

// 可见性
export enum VisibleTypeEnum {
  // 所有人可见
  Public = 1,
  // 仅自己可见
  Private = 2,
  // 好友可见
  Friend = 3,
}
