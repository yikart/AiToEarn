// 发布类型
export enum PubType {
  VIDEO = 'video', // 视频
  ARTICLE = 'article', // 文章
}

// 可见性
export enum VisibleTypeEnum {
  // 仅自己可见
  Private = 1,
  // 好友可见
  Friend = 2,
  // 所有人可见
  Public = 3,
}
