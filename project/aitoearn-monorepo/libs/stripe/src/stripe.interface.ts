export enum ICurrency {
  cny = 'cny', //  人民币
  usd = 'usd', //  美刀
  gbp = 'gbp', // 英镑
}

export enum RefundStatus {
  pending = 'pending',
  succeeded = 'succeeded',
  failed = 'failed',
  canceled = 'canceled',
}
