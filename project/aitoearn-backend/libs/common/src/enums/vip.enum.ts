export enum VipTier {
  Fortune = 'fortune',
  Treasure = 'treasure',
}

export enum VipStatus {
  Active = 'active',
  Expired = 'expired',
}

export const VipActiveStatusArr = [
  VipStatus.Active,
]
