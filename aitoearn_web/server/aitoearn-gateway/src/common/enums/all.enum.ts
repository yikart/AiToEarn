export enum HttpTags {
  DOC = 'doc',
}

export enum GenderEnum {
  MALE = 1,
  FEMALE = 2,
}

export enum ONOFF {
  ON = 1,
  OFF = 0,
}

export enum CheckStatus {
  PENDING = 0,
  PASS = 1,
  FAIL = -1,
}
